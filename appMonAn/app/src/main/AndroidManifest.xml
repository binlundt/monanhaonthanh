<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmonan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Resutl.choosemoney"></activity>
        <activity android:name="Resutl.resutl_make" />
        <activity android:name="Resutl.priceandmake" />
        <activity android:name=".Menutab" />
        <activity android:name="FragmentLayout.mainfood" />
        <activity android:name="Resutl.resutl_price" />
        <activity android:name="FragmentLayout.callmainscreen" />
        <activity android:name="Mainmenu.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>